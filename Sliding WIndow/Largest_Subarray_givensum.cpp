#include<iostream>
#include<vector>
using namespace std;

vector<int>largestSubarray(arr, int n, int k){
    int i = 0, j =0, 
    while(j<n){
        int sum = 0;
        
    }
}

int main(){
    vector<int> arr = { 5, 6, -5, 5, 3, 5, 3, -2, 0 };
    vector<int> ans(2);
    int k = 8;
    ans = lasrgestSubarray(arr, arr.size(), k);
    return 0;
}